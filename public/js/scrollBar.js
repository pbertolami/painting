/**
 * Created by Administrator on 5/10/2016.
 */

window.onload = function() {


//once again could not delte this file from my laravel app not sure why? so I commented out the code
    /*
    draw(0,0);

    function draw(x,y) {
        var myPad = document.getElementById("pad2");
        var context = myPad.getContext("2d");

        context.save();//save the canvas state
        context.clearRect(0,0,500,500);
        context.fillStyle = "#c08081";
        context.font = "30px Serif";
        context.textAlign = "center";
        context.textBaseline = "middle";
        context.fillText("Got Paint?", x, 200);
        context.restore();//restore the cnavas state
        x += 5; //increment the starting point of the text
        var timer = setTimeout('draw('+x+','+y+')',30);

    }
    */
};